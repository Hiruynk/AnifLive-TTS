from pathlib import Path

PROJECT_ROOT = Path(__file__).resolve().parents[3]
ASSETS_ROOT = PROJECT_ROOT / "data"
ARTIFACTS_ROOT = PROJECT_ROOT / "data" / "models"
REPORTS_ROOT = PROJECT_ROOT / "data" / "reports"
OUTPUTS_ROOT = PROJECT_ROOT / "data" / "reports" / "audio"

